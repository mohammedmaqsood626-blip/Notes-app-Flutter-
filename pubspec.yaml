name: notes_app
description: NoteFlow - A Notes Management Hybrid Application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  sqflite: ^2.3.0
  path: ^1.8.3
  shared_preferences: ^2.2.2
  intl: ^0.19.0
  flutter_staggered_grid_view: ^0.7.0
  uuid: ^4.3.3
  google_fonts: ^6.1.0
  flutter_slidable: ^3.0.1
  provider: ^6.1.1
  cupertino_icons: ^1.0.2

  hive: ^2.2.3
  hive_flutter: ^1.1.0

  file_picker: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true